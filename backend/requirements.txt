######################################################################
# Core Web-Backend (FastAPI + DB) ####################################
######################################################################
fastapi==0.110.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.29
psycopg2-binary==2.9.9          # PostgreSQL driver; switch to sqlite-browser if no PG
alembic==1.13.1
pydantic==2.7.0
python-multipart==0.0.9
requests==2.31.0
boto3==1.34.68                  # Only if you later copy files to S3

######################################################################
# Media / Audio helpers ##############################################
######################################################################
ffmpeg-python==0.2.0            # Python wrapper (FFmpeg exe must be in PATH)
pydub==0.25.1                   # Easy WAV/MP3 slicing & overlay
pyloudnorm==0.1.1               # LUFS loudness normalisation
srt==3.5.3                      # Generate .srt subtitle files
textgrid==1.5                   # Parse MFA TextGrid outputs

######################################################################
# Machine-Learning (ALL Token-Free) ##################################
######################################################################
# --- Speech Recognition --------------------------------------------
#  Whiper from OpenAI – public repo, no token required
git+https://github.com/openai/whisper.git@v20230314

# --- Translation ----------------------------------------------------
transformers==4.39.3            # HuggingFace, but we only pull PUBLIC MarianMT models
sentencepiece==0.1.99

# --- Text Cleaning --------------------------------------------------
language-tool-python==2.7.0      # Grammar fixes (bundles its own LanguageTool JAR)
langid==1.1.6                    # Language ID

# --- Text-to-Speech -------------------------------------------------
TTS                      # Coqui TTS – offline voices

# --- Forced Alignment / Lip-Sync -----------------------------------
montreal-forced-aligner==2.2.4   # CLI & Python helpers (token-free acoustic models)

######################################################################
# PyTorch + Audio backend (needed by Whisper, Coqui TTS, etc.) #######
######################################################################
torch==2.2.1                     # CPU build; install CUDA wheel if you have GPU
torchaudio==2.2.1
numpy==1.26.4

######################################################################
# Everything above installs with:  pip install -r requirements.txt  ##
######################################################################
