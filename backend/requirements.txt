
 ######################################################################
 fastapi
 uvicorn[standard]
 gunicorn
 sqlalchemy
 psycopg2-binary          # PostgreSQL driver; switch to sqlite-browser if no PG
 alembic
 pydantic
 python-multipart
 requests
 boto3                  # Only if you later copy files to S3
 pydantic-settings
 ######################################################################
 # Media / Audio helpers ##############################################
 ######################################################################
 ffmpeg-python            # Python wrapper (FFmpeg exe must be in PATH)
 pydub                   # Easy WAV/MP3 slicing & overlay
 pyloudnorm               # LUFS loudness normalisation
 srt                      # Generate .srt subtitle files
 textgrid                   # Parse MFA TextGrid outputs
 fasttext
 ######################################################################
 # Machine-Learning (ALL Token-Free) ##################################
 ######################################################################
 # --- Speech Recognition --------------------------------------------
 #  Whiper from OpenAI – public repo, no token required
 git+https://github.com/openai/whisper.git
 
 # --- Translation ----------------------------------------------------
 transformers            # HuggingFace, but we only pull PUBLIC MarianMT models
 sentencepiece
 
 # --- Text Cleaning --------------------------------------------------
 language-tool-python      # Grammar fixes (bundles its own LanguageTool JAR)
 langid                    # Language ID
 
 # --- Text-to-Speech -------------------------------------------------
 fasttext
 nltk
 indic-nlp-library
 indic-numtowords
 num2words
 g2p-en
 phonemizer
 styletts2
 gtts
 hifi-gan
 speechbrain 
 # --- Forced Alignment / Lip-Sync -----------------------------------
   # CLI & Python helpers (token-free acoustic models)
 montreal-forced-aligner                  # Alternative forced aligner
 
 ######################################################################
 # PyTorch + Audio backend (needed by Whisper, Coqui TTS, etc.) #######
 ######################################################################
 torch                     # CPU build; install CUDA wheel if you have GPU
 torchaudio
 numpy
 
 ######################################################################
 # Everything above installs with:  pip install -r requirements.txt  ##
 ######################################################################
