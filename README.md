# dubbing-ai

This project now bundles the [Sarvabhasha TTS](https://github.com/keshav-yoga/TTS2) pipeline for multilingual synthesis. Voices can be invoked by using a `voice_name` beginning with `sarva_` followed by the language code.
